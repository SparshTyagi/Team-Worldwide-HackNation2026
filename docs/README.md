# Documentation Index

This folder hosts operational and architecture docs for the reorganized repo.

## Primary docs

- Root architecture roadmap: `../Roadmap.md`
- Root run/setup guide: `../setup.md`
- Root project operator guide: `../README.md`

## Migration note

The repo was restructured into:

- `apps/web` (from `frontend`)
- `apps/api` (from `server`)
- `packages/intent-engine` (from `client`)
- `infra/supabase_schema.sql` (from `backend/supabase_schema.sql`)

Use new paths for scripts, imports, and local run instructions.
