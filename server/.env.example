# Copy to .env and fill in (never commit .env).
# Revoke any key that was ever pasted into chat or committed.
# This file powers `node src/index.js` backend runtime.

OPENROUTER_API_KEY=
OPENROUTER_OFFER_MODEL=nvidia/nemotron-3-super:free
OPENROUTEROFFERMODEL=nvidia/nemotron-3-super:free

# Backward-compatible alias (optional)
# OPENROUTER_MODEL=nvidia/nemotron-3-super:free

# Optional
# PORT=8080
# HOST=0.0.0.0
